# Important Notes:
# Every field you add must be added to buildSrc/src/main/groovy/multiloader-common.gradle expandProps map.

# Project
version=3.0
group=com.turbocrackers.bettervillagespawnpoint

# Common
minecraft_version=1.12.2
mod_name=BetterVillageSpawnPoint
mod_author=TurboCrackers
mod_id=bettervillagespawnpoint
license=CC0-1.0
credits=
description=A mod that automatically sets the spawn point to a reasonable spot in the nearest village from a configurable list of village IDs\n\n(example: minecraft:village_plains) and/or tags (example: #minecraft:villages).\n\nIt tries really hard to put you on the ground if it can, as opposed to other mods that will spawn you in a tree or on an island above a village.
# Each branch ships a jar built and tested against exactly one Minecraft release. The API moves
# between patch releases (1.21.9 spawn rework, 1.21.11 Identifier rename, 26.2 tag changes), so a
# wider range only turns a clean loader refusal into a crash at startup.
minecraft_version_range=[1.12.2]

# 1.12.2 is Forge only. There is no common module: Minecraft has no official mappings before 1.14,
# so VanillaGradle cannot build one, and Legacy Fabric is not offered for it by Prism. The
# code lives in forge/ under the same packages as the common module on the newer branches.

# Forge
# https://files.minecraftforge.net/net/minecraftforge/forge/
forge_version=14.23.5.2864
forge_loader_version_range=[14.23.5.2860,)
# MCP names for 1.12.2 (ForgeGradle 'stable' channel); there are no official Mojang mappings.
mcp_mappings_channel=stable
mcp_mappings_version=39-1.12

# Gradle
org.gradle.jvmargs=-Xmx3G -Djava.net.preferIPv4Stack=true
org.gradle.daemon=false

# Java
java_version=8
java_version_name=JAVA_8
