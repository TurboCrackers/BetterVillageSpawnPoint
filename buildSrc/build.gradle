plugins {
    id 'groovy-gradle-plugin'
}

repositories {
    mavenCentral()
}

dependencies {
    // Used by common's checkMixinTargets task to scan the Minecraft jar for callers of each hook.
    implementation 'org.ow2.asm:asm:9.10.1'
}
